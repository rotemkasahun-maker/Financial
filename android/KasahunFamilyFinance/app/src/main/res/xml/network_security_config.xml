<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        LOCAL TEST ONLY / TODO REMOVE before production.
        Allows cleartext HTTP traffic to the host machine from the emulator.
    -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
